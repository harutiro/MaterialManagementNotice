<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.makino.harutiro.materialmanagementnotice">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="app.makino.harutiro.materialmanagementnotice.RealmApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShareTN">

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5364758522568684~1748922401"/>

        <activity android:name="app.makino.harutiro.materialmanagementnotice.SelectTagActivity" />
        <activity android:name="app.makino.harutiro.materialmanagementnotice.EditTagActivity" />
        <activity android:name="app.makino.harutiro.materialmanagementnotice.EditActivity"/>
        <activity android:name="app.makino.harutiro.materialmanagementnotice.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>